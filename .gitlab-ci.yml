image: node:7

stages:
  - deploy


deploy_staging:
  stage: deploy
  image: ruby:latest
  script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=duo2-frontend-staging --api-key=$HEROKU_API_KEY
  only:
    - master

