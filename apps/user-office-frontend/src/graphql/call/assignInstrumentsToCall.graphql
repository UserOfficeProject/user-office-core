mutation assignInstrumentsToCall($instrumentIds: [Int!]!, $callId: Int!) {
  assignInstrumentsToCall(
    assignInstasdadrumentsToCallInput: {
      instrumentIds: $instrumentIds
      callId: $callId
    }
  ) {
    rejection {
      ...rejection
    }
    call {
      id
    }
  }
}
