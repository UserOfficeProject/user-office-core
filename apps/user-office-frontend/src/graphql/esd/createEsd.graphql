mutation createEsd(
  $esiId: Int!
  $evaluation: EsdEvaluation!
  $comment: String!
) {
  createEsd(esiId: $esiId, evaluation: $evaluation, comment: $comment) {
    esd {
      ...esd
      reviewer {
        ...basicUserDetails
      }
    }
    rejection {
      ...rejection
    }
  }
}
