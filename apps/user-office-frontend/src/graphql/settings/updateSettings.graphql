mutation updateSettings($input: UpdateSettingsInput!) {
  updateSettings(updatedSettingsInput: $input) {
    settings {
      id
      settingsValue
      description
    }
    rejection {
      ...rejection
    }
  }
}
