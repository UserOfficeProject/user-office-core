[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act, con, eft

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.sub == p.sub && r.obj.type == p.obj && r.act == p.act && (p.con == "" || evalCondition(r.sub, r.obj, p.con))
