query getCallSubmissionDetails($callId: Int!) {
  call(callId: $callId) {
    proposalWorkflowId
    submissionMessage
    workflow {
      workflowConnections {
        prevStatusId
        status {
          shortCode
        }
      }
    }
  }
}
