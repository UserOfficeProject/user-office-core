query getCallSubmissionDetails($callId: Int!) {
  call(callId: $callId) {
    proposalWorkflowId
    submissionMessage
    proposalWorkflow {
      workflowConnections {
        prevStatusId
        status {
          shortCode
        }
      }
    }
  }
}
