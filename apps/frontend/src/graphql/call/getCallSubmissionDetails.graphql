query getCallSubmissionDetails($callId: Int!) {
  call(callId: $callId) {
    proposalWorkflowId
    submissionMessage
    proposalWorkflow {
      connections {
        ...workflowConnection
      }
      statuses {
        ...workflowStatus
      }
    }
  }
}
