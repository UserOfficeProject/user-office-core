query getCallSubmissionDetails($callId: Int!) {
  call(callId: $callId) {
    proposalWorkflowId
    submissionMessage
    proposalWorkflow {
      workflowConnectionGroups {
        connections {
          prevStatusId
          status {
            shortCode
          }
        }
      }
    }
  }
}
