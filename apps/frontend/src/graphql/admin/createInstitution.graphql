mutation createInstitution($name: String!, $country: Int!, $rorId: String) {
  createInstitution(name: $name, country: $country, rorId: $rorId) {
    id
    name
    country {
      ...country
    }
    rorId
  }
}
