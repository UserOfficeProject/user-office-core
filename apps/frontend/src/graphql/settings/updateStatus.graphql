mutation updateStatus($id: String!, $name: String!, $description: String!) {
  updateStatus(
    updatedStatusInput: { id: $id, name: $name, description: $description }
  ) {
    ...status
  }
}
