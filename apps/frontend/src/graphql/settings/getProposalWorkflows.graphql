query getProposalWorkflows {
  proposalWorkflows {
    id
    name
    description
    proposalWorkflowConnectionGroups {
      connections {
        status {
          shortCode
        }
      }
    }
  }
}
