mutation updateWorkflow($id: Int!, $name: String!, $description: String!, $connectionLineType: String) {
  updateWorkflow(
    updatedWorkflowInput: { id: $id, name: $name, description: $description, connectionLineType: $connectionLineType }
  ) {
    id
    name
    description
    connectionLineType
    workflowConnections {
      id
      sortOrder
      workflowId
      statusId
      status {
        id
        name
        description
      }
      nextStatusId
      prevStatusId
      prevConnectionId
      posX
      posY
      statusChangingEvents {
        statusChangingEventId
        workflowConnectionId
        statusChangingEvent
      }
    }
  }
}
