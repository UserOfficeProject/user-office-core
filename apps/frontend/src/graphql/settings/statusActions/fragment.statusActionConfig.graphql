fragment statusActionDefaultConfig on StatusActionDefaultConfig {
  ... on EmailActionDefaultConfig {
    recipients {
      name
      description
    }

    emailTemplates {
      ...emailTemplate
    }
  }
  ... on RabbitMQActionDefaultConfig {
    exchanges
  }
}
