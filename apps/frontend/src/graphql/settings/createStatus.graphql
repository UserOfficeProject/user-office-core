mutation createStatus(
  $id: String!
  $name: String!
  $description: String!
  $entityType: WorkflowType!
) {
  createStatus(
    newStatusInput: {
      id: $id
      name: $name
      description: $description
      entityType: $entityType
    }
  ) {
    ...status
  }
}
