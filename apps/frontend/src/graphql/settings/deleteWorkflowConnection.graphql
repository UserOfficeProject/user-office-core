mutation deleteWorkflowConnection($connectionId: Int!) {
  deleteWorkflowConnection(connectionId: $connectionId) {
    id
    sortOrder
    workflowId
    statusId
    nextStatusId
    prevStatusId
    posX
    posY
  }
}
