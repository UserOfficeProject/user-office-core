mutation addStatusToWorkflow(
  $workflowId: Int!
  $statusId: Int!
  $posX: Int!
  $posY: Int!
) {
  addStatusToWorkflow(
    newWorkflowStatusInput: {
      workflowId: $workflowId
      statusId: $statusId
      posX: $posX
      posY: $posY
    }
  ) {
    id
  }
}
