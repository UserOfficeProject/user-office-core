query getWorkflows($entityType: WorkflowType!) {
  workflows(entityType: $entityType) {
    id
    name
    description
    connections {
      ...workflowConnection
    }

    statuses {
      ...workflowStatus
    }
  }
}
