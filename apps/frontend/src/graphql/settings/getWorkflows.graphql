query getWorkflows($entityType: String!) {
  workflows(entityType: $entityType) {
    id
    name
    description
    workflowConnectionGroups {
      connections {
        status {
          shortCode
        }
      }
    }
  }
}
