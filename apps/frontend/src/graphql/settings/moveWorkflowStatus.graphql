mutation moveWorkflowStatus(
  $from: IndexWithGroupId!
  $to: IndexWithGroupId!
  $workflowId: Int!
) {
  moveWorkflowStatus(
    moveWorkflowStatusInput: { from: $from, to: $to, workflowId: $workflowId }
  ) {
    id
  }
}
