mutation assignProposalsToInstrument(
  $proposals: [ProposalSelectionInput!]!
  $instrumentIds: [Int!]!
) {
  assignProposalsToInstrument(
    proposals: $proposals
    instrumentIds: $instrumentIds
  )
}
