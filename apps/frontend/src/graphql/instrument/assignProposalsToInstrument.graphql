mutation assignProposalsToInstruments(
  $proposals: [ProposalSelectionInput!]!
  $instrumentIds: [Int!]!
) {
  assignProposalsToInstruments(
    proposals: $proposals
    instrumentIds: $instrumentIds
  )
}
