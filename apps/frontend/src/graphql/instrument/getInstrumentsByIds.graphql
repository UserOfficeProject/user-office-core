query getInstrumentsByIds($instrumentIds: [Int!]!) {
  instrumentsByIds(instrumentIds: $instrumentIds) {
    id
    instrumentContact {
      id
      firstname
      lastname
      preferredname
    }
    scientists {
      id
      firstname
      lastname
      preferredname
    }
  }
}
