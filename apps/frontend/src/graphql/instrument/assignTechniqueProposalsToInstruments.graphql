mutation assignTechniqueProposalsToInstruments(
  $proposalPks: [Int!]!
  $instrumentIds: [Int!]!
) {
  assignTechniqueProposalsToInstruments(
    proposalPks: $proposalPks
    instrumentIds: $instrumentIds
  )
}
