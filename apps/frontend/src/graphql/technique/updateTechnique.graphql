mutation updateTechnique(
  $techniqueId: Int!
  $name: String!
  $shortCode: String!
  $description: String!
) {
  updateTechnique(
    id: $techniqueId
    name: $name
    shortCode: $shortCode
    description: $description
  ) {
    ...technique
  }
}
