mutation changeXpressProposalsStatus(
  $proposalPks: [Int!]!
  $statusId: Int!
) {
  changeXpressProposalsStatus(
    changeProposalsStatusInput: { proposalPks: $proposalPks, statusId: $statusId }
  )
}
