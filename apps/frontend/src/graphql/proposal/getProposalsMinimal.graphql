query getProposalsMinimal($filter: ProposalsFilter) {
  proposals(filter: $filter) {
    proposals {
      primaryKey
      proposalId
      title
      submitted
      status {
        name
      }
    }
  }
}
