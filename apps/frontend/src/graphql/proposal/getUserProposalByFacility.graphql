query getProposalsByUserFacility(
  $filter: ProposalsFilter
  $offset: Int
  $first: Int
) {
  proposalsByUserFacility(
    filter: $filter
    offset: $offset
    first: $first
  ) {
    proposals {
      primaryKey
      proposalId
      title
      submitted
      finalStatus
      statusName
      allocationTimeUnit
      callShortCode
      instruments {
        id
        name
      }
      faps {
        code
      }
      principalInvestigator {
        firstname
        lastname
        preferredname
        institutionId
        email
      }
    }
    totalCount
  }
}
