query getFapReviewTemplates($filter: FapReviewTemplatesFilter) {
  fapReviewTemplates(filter: $filter) {
    templateId
    name
    description
    isArchived
    questionaryCount
    callCount
  }
}
