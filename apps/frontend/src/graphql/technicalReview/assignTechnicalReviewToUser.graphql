mutation updateTechnicalReviewAssignee(
  $proposalPks: [Int!]!
  $userId: Int!
  $instrumentId: Int!
) {
  updateTechnicalReviewAssignee(
    proposalPks: $proposalPks
    userId: $userId
    instrumentId: $instrumentId
  ) {
    ...coreTechnicalReview
  }
}
