query getRolesByTags($tagIds: [Int!]) {
  rolesByTags(tagIds: $tagIds) {
    id
    shortCode
    title
    description
    isRootRole
    config {
      ...roleConfig
    }
    tags {
      id
      name
    }
  }
}