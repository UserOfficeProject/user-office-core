query getUserWithRoles($userId: Int!) {
  user(userId: $userId) {
    firstname
    lastname
    roles {
      id
      shortCode
      title
      description
      dataAccess
      permissions
    }
  }
}
