mutation externalTokenLogin(
  $externalToken: String!
  $redirectUri: String!
  $iss: String
) {
  externalTokenLogin(
    externalToken: $externalToken
    redirectUri: $redirectUri
    iss: $iss
  )
}
