mutation createFeedback($scheduledEventId: Int!) {
  createFeedback(scheduledEventId: $scheduledEventId) {
    ...feedback
    questionary {
      ...questionary
      isCompleted
    }
  }
}
