mutation addCasbinPolicy(
  $subject: String!
  $resource: String!
  $action: String!
  $condition: String!
) {
  addCasbinPolicy(
    subject: $subject
    resource: $resource
    action: $action
    condition: $condition
    effect: "allow"
  )
}
