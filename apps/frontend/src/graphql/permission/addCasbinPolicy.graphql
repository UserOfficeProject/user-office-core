mutation addCasbinPolicy(
  $subject: String!
  $object: String!
  $action: String!
  $condition: String!
) {
  addCasbinPolicy(
    subject: $subject
    object: $object
    action: $action
    condition: $condition
    effect: "allow"
  )
}
