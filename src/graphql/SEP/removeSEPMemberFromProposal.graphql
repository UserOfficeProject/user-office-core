mutation removeMemberFromSEPProposal(
  $memberId: Int!
  $sepId: Int!
  $proposalId: Int!
) {
  removeMemberFromSEPProposal(
    memberId: $memberId
    sepId: $sepId
    proposalId: $proposalId
  ) {
    error
    sep {
      id
    }
  }
}
