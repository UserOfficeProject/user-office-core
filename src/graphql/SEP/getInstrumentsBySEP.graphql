query getInstrumentsBySEP($sepId: Int!, $callId: Int!) {
  instrumentsBySep(sepId: $sepId, callId: $callId) {
    instrumentId
    name
    shortCode
    description
    availabilityTime
    scientists {
      ...basicUserDetails
    }
  }
}
