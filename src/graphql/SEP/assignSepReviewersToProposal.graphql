mutation assignSepReviewersToProposal(
  $memberIds: [Int!]!
  $sepId: Int!
  $proposalId: Int!
) {
  assignSepReviewersToProposal(
    memberIds: $memberIds
    sepId: $sepId
    proposalId: $proposalId
  ) {
    rejection {
      ...rejection
    }
    sep {
      id
    }
  }
}
