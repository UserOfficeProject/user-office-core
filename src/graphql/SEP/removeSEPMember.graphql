mutation removeMemberFromSep(
  $memberId: Int!
  $sepId: Int!
  $roleId: UserRole!
) {
  removeMemberFromSep(memberId: $memberId, sepId: $sepId, roleId: $roleId) {
    error
    sep {
      id
    }
  }
}
