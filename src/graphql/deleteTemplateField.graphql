mutation deleteTemplateField($id: String!) {
  deleteTemplateField(id: $id) {
    template {
      steps {
        topic {
          topic_title
          topic_id
        }
        fields {
          ...proposalTemplateField
        }
      }
    }
    error
  }
}
