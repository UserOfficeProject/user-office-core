query getReview($reviewId: Int!, $sepId: Int) {
  review(reviewId: $reviewId, sepId: $sepId) {
    ...coreReview
    proposal {
      id
      title
      abstract
      proposer {
        id
      }
    }
  }
}
