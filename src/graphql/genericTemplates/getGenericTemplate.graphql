query getGenericTemplate($genericTemplateId: Int!) {
  genericTemplate(genericTemplateId: $genericTemplateId) {
    ...genericTemplate
    questionary {
      isCompleted
      ...questionary
    }
  }
}
