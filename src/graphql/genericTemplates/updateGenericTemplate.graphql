mutation updateGenericTemplate($sampleId: Int!, $title: String) {
  updateGenericTemplate(sampleId: $genericTemplateId, title: $title) {
    genericTemplate {
      ...genericTemplate
    }
    rejection {
      ...rejection
    }
  }
}
