mutation updateProposalFiles(
  $proposal_id: Int!
  $question_id: String!
  $files: [String!]!
) {
  updateProposalFiles(
    proposal_id: $proposal_id
    question_id: $question_id
    files: $files
  ) {
    files
    error
  }
}
