query userWithReviews($id: Int!) {
  user(id: $id) {
    id
    firstname
    lastname
    organisation
    reviews {
      id
      comment
      grade
      proposal {
        id
        title
      }
    }
  }
}
