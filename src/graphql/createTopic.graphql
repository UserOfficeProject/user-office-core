mutation createTopic($sortOrder: Int!) {
  createTopic(sortOrder: $sortOrder) {
    template {
      steps {
        topic {
          topic_title
          topic_id
        }
        fields {
          ...proposalTemplateField
        }
      }
    }
    error
  }
}
