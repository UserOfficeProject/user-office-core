mutation assignProposalsToInstrument(
  $proposals: [ProposalsToInstrumentArgs!]!
  $instrumentId: Int!
) {
  assignProposalsToInstrument(
    proposals: $proposals
    instrumentId: $instrumentId
  ) {
    error
    isSuccess
  }
}
