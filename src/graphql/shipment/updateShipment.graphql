mutation updateShipment(
  $shipmentId: Int!
  $title: String
  $proposalId: Int
  $status: ShipmentStatus
) {
  updateShipment(
    shipmentId: $shipmentId
    title: $title
    status: $status
    proposalId: $proposalId
  ) {
    rejection {
      ...rejection
    }
    shipment {
      ...shipment
      questionary {
        ...questionary
      }
    }
  }
}
