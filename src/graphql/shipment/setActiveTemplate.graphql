mutation setActiveTemplate(
  $templateCategoryId: TemplateCategoryId!
  $templateId: Int!
) {
  setActiveTemplate(
    templateId: $templateId
    templateCategoryId: $templateCategoryId
  ) {
    isSuccess
    error
  }
}
