mutation setActiveTemplate(
  $templateGroupId: TemplateGroupId!
  $templateId: Int!
) {
  setActiveTemplate(
    templateId: $templateId
    templateGroupId: $templateGroupId
  ) {
    isSuccess
    rejection {
      ...rejection
    }
  }
}
