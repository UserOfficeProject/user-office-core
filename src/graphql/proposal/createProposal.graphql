mutation createProposal($callId: Int!) {
  createProposal(callId: $callId) {
    proposal {
      id
      status {
        ...proposalStatus
      }
      shortCode
      questionaryId
      questionary {
        ...questionary
      }
      proposer {
        ...basicUserDetails
      }
      users {
        ...basicUserDetails
      }
      samples {
        ...sample
      }
    }
    rejection {
      ...rejection
    }
  }
}
