mutation createProposal($callId: Int!) {
  createProposal(callId: $callId) {
    proposal {
      id
      status {
        id
        name
        description
      }
      shortCode
      questionaryId
      questionary {
        ...questionary
      }
    }
    error
  }
}
