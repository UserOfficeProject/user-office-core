query getBlankProposal($callId: Int!) {
  blankProposal(callId: $callId) {
    id
    status
    shortCode
    rankOrder
    finalStatus
    title
    abstract
    created
    updated
    callId
    templateId
    proposer {
      ...basicUserDetails
    }
    questionary {
      ...questionary
    }
    users {
      ...basicUserDetails
    }
    reviews {
      id
      grade
      comment
      status
      userID
      reviewer {
        firstname
        lastname
        username
        id
      }
    }
  }
}
