mutation updateProposalFiles(
  $proposalId: Int!
  $questionId: String!
  $files: [String!]!
) {
  updateProposalFiles(
    proposalId: $proposalId
    questionId: $questionId
    files: $files
  ) {
    files
    error
  }
}
