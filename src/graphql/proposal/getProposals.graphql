query getProposals($filter: String!) {
  proposals(filter: $filter) {
    proposals {
      id
      shortCode
      title
      status
      technicalReview{
        timeAllocation
        status
      }
    }
    totalCount
  }
}
