query getUserProposals {
  me {
    proposals {
      id
      shortCode
      title
      status {
        id
        name
        description
      }
      statusId
      created
      finalStatus
      notified
      submitted
    }
  }
}
