query getUserProposals {
  me {
    proposals {
      id
      shortCode
      title
      status {
        id
        name
        description
      }
      publicStatus
      statusId
      created
      finalStatus
      notified
      submitted
    }
  }
}
