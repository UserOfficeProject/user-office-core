mutation updateTopic(
  $topicId: Int!
  $title: String
  $sortOrder: Float
  $isEnabled: Boolean
) {
  updateTopic(
    id: $topicId
    title: $title
    sortOrder: $sortOrder
    isEnabled: $isEnabled
  ) {
    topic {
      ...topic
    }
    error
  }
}
