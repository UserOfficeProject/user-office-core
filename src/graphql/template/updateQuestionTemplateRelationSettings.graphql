mutation updateQuestionTemplateRelationSettings(
  $questionId: String!
  $templateId: Int!
  $config: String
  $dependencies: [FieldDependencyInput!]!
) {
  updateQuestionTemplateRelationSettings(
    questionId: $questionId
    templateId: $templateId
    config: $config
    dependencies: $dependencies
  ) {
    template {
      ...template
    }
    error
  }
}
