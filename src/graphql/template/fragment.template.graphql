fragment template on Template {
  steps {
    topic {
      title
      id
    }
    fields {
      ...questionRel
    }
  }
  templateId
  categoryId
  name
  description
  complementaryQuestions {
    ...question
  }
}
