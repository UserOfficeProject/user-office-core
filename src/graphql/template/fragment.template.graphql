fragment template on Template {
  steps {
    topic {
      title
      id
    }
    fields {
      ...questionRel
    }
  }
  templateId
  name
  description
  complementaryQuestions {
    ...question
  }
}
