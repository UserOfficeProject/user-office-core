fragment template on Template {
  steps {
    topic {
      title
      id
      sortOrder
    }
    fields {
      ...questionTemplateRelation
    }
  }
  templateId
  categoryId
  name
  description
  complementaryQuestions {
    ...question
  }
}
