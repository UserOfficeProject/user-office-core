fragment template on Template {
  steps {
    topic {
      ...topic
    }
    fields {
      ...questionTemplateRelation
    }
  }
  isArchived
  questionaryCount
  templateId
  categoryId
  name
  description
  complementaryQuestions {
    ...question
  }
}
