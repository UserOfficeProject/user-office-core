mutation cloneTemplate($templateId: Int!) {
  cloneTemplate(templateId: $templateId) {
    template {
      ...templateMetadata
      questionaryCount
    }
    rejection {
      ...rejection
    }
  }
}
