overwrite: true
schema: "<SCHEMA_URL>"
documents: "src/graphql/*.{graphql,fragment}"
generates:
  src/generated/sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
