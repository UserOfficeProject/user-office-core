overwrite: true
schema: "<SCHEMA_URL>"
documents: "src/graphql/**/*.graphql"
generates:
  src/generated/sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
